#Replace the double space from problem 3 with single spaces.

name = "Shiavnsh is  a good  boy  and"

print(name.replace("  " , " "))

#Strings are immutable which means that you cannot change them by running functions on them.

#In simple words  name  variable doesnt change,   by appling methods on it it just  make new modified string.